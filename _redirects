# Cloudflare Pages Redirects and Headers
# Static files are served directly, no redirects needed
# Only redirect HTML routes that don't match actual files

# API proxy (if needed)
# /api/*  https://your-vps-domain.com/api/:splat  200

# Security headers (exclude manifest.json and sw.js from strict headers)
/manifest.json
  Access-Control-Allow-Origin: *
  Access-Control-Allow-Methods: GET, OPTIONS
  Access-Control-Allow-Headers: *
  Content-Type: application/manifest+json

/sw.js
  Access-Control-Allow-Origin: *

# Security headers for other files
/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  X-XSS-Protection: 1; mode=block
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), microphone=(), camera=()

















