# Cloudflare Pages Configuration
# This file is used for Cloudflare Pages deployment (Frontend)

name = "audio-city"
compatibility_date = "2024-01-01"

[env.production]
name = "audio-city"

# Pages configuration
pages_build_output_dir = "."

# Exclude large files and backend directories
[build]
command = ""

# Environment variables (set these in Cloudflare dashboard)
# [vars]
# API_URL = "https://your-vps-domain.com"
# WORKER_URL = "https://audio-city-api-proxy.YOUR_SUBDOMAIN.workers.dev"

